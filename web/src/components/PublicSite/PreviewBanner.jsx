export function PreviewBanner() {
  return (
    <div className="bg-amber-50 border-b border-amber-100 text-amber-900">
      <div className="max-w-7xl mx-auto px-6 py-3 text-sm font-semibold">
        Preview mode (draft). Publish from the dashboard when you're ready.
      </div>
    </div>
  );
}
